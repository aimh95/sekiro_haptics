add_executable(sekiro_action_feedback main.cpp)
target_link_libraries(sekiro_action_feedback PRIVATE sekiro_haptics)
if(TARGET sekiro_haptics_dualsense_transport)
    target_link_libraries(sekiro_action_feedback PRIVATE sekiro_haptics_dualsense_transport)
    target_compile_definitions(sekiro_action_feedback PRIVATE SH_ACTION_HID=1)
endif()
if(TARGET sekiro_haptics_windows_audio)
    target_link_libraries(sekiro_action_feedback PRIVATE sekiro_haptics_windows_audio)
    target_compile_definitions(sekiro_action_feedback PRIVATE SH_ACTION_AUDIO=1)
endif()
